const request=require('supertest'); const app=require('../src/app'); describe('RV Park app (consolidated)',()=>{ it('health endpoint exists',async()=>{const res=await request(app).get('/health'); expect([200,500]).toContain(res.status);}); it('renders search page with bootstrap',async()=>{const res=await request(app).get('/'); expect(res.status).toBe(200); expect(res.text.toLowerCase()).toMatch(/bootstrap/); expect(res.text).toMatch(/Search Sites/);});});